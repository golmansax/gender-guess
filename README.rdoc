== Gender Guess

Small Rails app that asks user for height and weight and tries to guess gender
from that information.

*TODOs:*
- Add unit (lb/kg/cm/in.) checking
- Make site look prettier
- Display stats on success rate

<b>Built with:</b> Rails, Compass (Sass), SQLite, Redis
